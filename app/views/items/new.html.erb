<div class="title">
    <h3 class="fas fa-crown nav-link ">
        <span class="font">New Post</span>
    </h3>
</div>
<div class="container">
  <% if @item.errors.any? %>
    <%= @item.errors.count %>件のエラーが発生しました。
    <% @item.errors.full_messages.each do |message| %>
      <%= message %>
    <% end %>
  <% end %>
  <div class="row">
    <div class="col-4">
    </div>
    <div class="col-6">
      <%= form_with model: @item, url: items_comfirm_path, method: :post, local:true do |f| %>
        <h4>投稿名</h4>
        <%= f.text_field :name %>
        <h4>投稿画像</h4>
        <%= f.attachment_field :image %>
        <h4>コメント</h4>
        <%= f.text_area :introduction %>
        <h4>タグ</h4>
        <h6>（複数ある場合は”(カンマ)”で区切ってください）</h6>
        <%= f.text_area :tag %>
        <%= @tags %>
        <h4>販売価格（この画像を出品する場合のみ）</h4>
        <%= f.text_field :price %>円</br>
        <pre></pre>
        <%= f.submit '投稿する', class:"btn btn-dark" %>
      <% end %>
      <div class="col-2">
      </div>
    </div>
  </div>
</div>