<div class="title">
    <h3 class="fas fa-crown nav-link ">
        <span class="font"> Your Post Edit</span>
    </h3>
</div>
<div class="container">
  <h4>＊投稿内容を変更し、間違いがなければ「変更」を押してください</h4>
  <div class="row">
    <div class="col-6">
      <%= form_with model: @item, url: item_path(@item), local: true do |f| %>
        <h4>投稿画像</h4>
        <%= f.attachment_field :image %>
        <h4>タイトル</h4>
        <%= f.text_field :name %>
        <h4>投稿説明</h4>
        <%= f.text_area :introduction %>
        <h4>タグ</h4>
        <%= f.text_area :tag, value: @tags %>
        <h4>値段</h4>
        <%= f.text_field :price %>円
        <h4></h4>
        <%= f.submit '変更', class:"btn btn-success" %>
      <% end %>
    </div>
    <div>
      <% if @item.user == current_user %>
       <%= link_to "削除する", item_path(@item), method: :delete %>
      <% end %>
    </div> 
    <div class="col-2">
    </div>
  </div>
</div>