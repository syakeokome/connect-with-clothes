<h1>出品履歴一覧</h1>
<% @items.each do |item|%>
    <div>
        <%= link_to item_path(item) do %>
          <%= attachment_image_tag item, :image, :fill, 100, 100 %>
        <% end %>
    </div>
<% end %>
