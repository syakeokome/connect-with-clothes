<div class="parents">
    <% items.each do |item|%>
        <div>
            <%= link_to item_path(item) do %>
              <%= attachment_image_tag item, :image, :fill, 300, 300 %>
            <% end %>
        </div>
    <% end %>
</div>